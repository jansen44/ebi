on: [push]

jobs:
  test:
    name: Ebi Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable

      - uses: actions-rs/cargo@v1
        with:
          command: build

      - name: LS
        run: |
          ls .
          ls target/
          ls target/debug/

      - uses: actions-rs/cargo@v1
        with:
          command: test
          args: -- --nocapture
